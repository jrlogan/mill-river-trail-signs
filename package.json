{
  "name": "mrt-signs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mill River Trail interpretive signage — bilingual content, print artwork, and web pages generated from one source.",
  "scripts": {
    "map": "node build/make-map.mjs",
    "images": "node build/make-web-images.mjs",
    "check": "node build/check-links.mjs",
    "sign": "node build/render-sign.mjs",
    "site": "node build/render-site.mjs",
    "all": "node build/make-map.mjs && node build/make-web-images.mjs && node build/render-sign.mjs && node build/render-site.mjs && node build/check-links.mjs"
  },
  "dependencies": {
    "jsqr": "^1.4.0",
    "pngjs": "^7.0.0",
    "puppeteer": "^23.0.0",
    "qrcode": "^1.5.4",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
